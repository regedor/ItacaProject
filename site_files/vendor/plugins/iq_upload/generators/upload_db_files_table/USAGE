Description:
    Generates migration to create db_files table used with DB attachment store.

Example:
    ./script/generate upload_db_files_table

    This will create an Tail worker:
        Migration:      db/migrate/{NEXT_MIGRATION_NUM}_add_upload_db_files_table.rb
